#include <stdio.h>
#include <string.h>
#include <arpa/inet.h>

int main(int argc, char *argv[])
{
    struct sockadddr_un server;
    int sock;

    if(sock=socket(server.sun_patch,))


    return 0;
}