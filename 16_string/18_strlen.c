#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main ( int argc, char *argv[] )
{
    char *p = NULL;
    char buf[16] = {0};

    int ret1=0,ret2=0;
    //ret1 = strlen(p);
    ret2 = strlen(buf);
 
    return 0;
}